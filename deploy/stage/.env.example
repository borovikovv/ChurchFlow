# Immutable image tags. Use a commit SHA tag produced by Build deployment images.
API_IMAGE=ghcr.io/borovikovv/churchflow-api:<commit-sha>
MIGRATOR_IMAGE=ghcr.io/borovikovv/churchflow-api-migrator:<commit-sha>
WEB_IMAGE=ghcr.io/borovikovv/churchflow-web:<commit-sha>

# Web runtime and build-time values. NEXT_PUBLIC_* are embedded into the web image at build time.
NEXT_PUBLIC_WEB_URL=https://stage.mychurchflow.org
NEXT_PUBLIC_API_URL=https://api-stage.mychurchflow.org/v1
API_INTERNAL_URL=http://churchflow-stage-api:4000/v1
COOKIE_DOMAIN=.mychurchflow.org
JWT_ACCESS_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----\n"
